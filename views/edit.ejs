<!DOCTYPE html>
<html lang="en">
<%- include ('./partials/head.ejs', {page: 'Update Country Info'}) %>
<body class="has-navbar-fixed-top has-background-black-ter">
    <%- include ('./partials/nav.ejs') %>
    <section class="hero has-background-primary-dark has-text-centered">
        <div class="hero-body">
            <p class="title has-text-black-bis">Update Country Info: </p>
            <h4 class="title is-4 has-text-black-bis"><%= country.name %></h4>
        </div>
    </section>
    <main class="container">
        <div class="columns is-multiline is-centered">
            <div class="column is-two-thirds">
                <article class="card form-card has-background-grey-dark">
                    <form action="/countries/<%= index %>?_method=PUT" method="POST">
                        <div class="field">
                            <label class="label has-text-light">Country Name</label>
                            <div class="control">
                                <input class="input" type="text" name="name" placeholder="<%= country.name %>" value="<%= country.name %>">
                            </div>
                        </div>
                        <div class="field">
                            <label class="label has-text-light">Population</label>
                            <div class="control">
                                <input class="input" type="text" name="population" placeholder="<%= country.population %>" value="<%= country.population %>">
                            </div>
                        </div>
                        <div class="field">
                            <label class="label has-text-light">Demonym</label>
                            <div class="control">
                                <input class="input" type="text" name="demonym" placeholder="<%= country.demonym %>" value="<%= country.demonym %>">
                            </div>
                        </div>
                        <div class="field">
                            <label class="label has-text-light">Capital</label>
                            <div class="control">
                                <input class="input" type="text" name="capital" placeholder="<%= country.capital %>" value="<%= country.capital %>">
                            </div>
                        </div>
                        <div class="field">
                            <label class="label has-text-light">Largest City</label>
                            <div class="control">
                                <input class="input" type="text" name="largestCity" placeholder="<%= country.largestCity %>" value="<%= country.largestCity %>">
                            </div>
                        </div>
                        <div class="field">
                            <label class="label has-text-light">Flag Image</label>
                            <div class="control">
                                <input class="input" type="text" name="flagImg" placeholder="<%= country.flagImg %>" value="<%= country.flagImg %>">
                            </div>
                        </div>
                        <div class="field form-buttons is-grouped is-grouped-centered">
                            <div class="control">
                                <button class="button is-info" type="submit">Update Country Info</button>
                            </div>
                        </div>
                    </form>
                    <form action="/countries/<%= index %>?_method=DELETE" method="POST">
                        <div class="field is-grouped is-grouped-centered">
                            <div class="control">
                                <button type="submit" class="button is-danger has-background-danger-dark">Remove Country</button>
                            </div>
                            <div class="control">
                                <a class="button is-light" href="/countries/<%= index %>">Cancel</a>
                            </div>
                        </div>
                    </form>
                </article>
            </div>
        </div>
    </main>
</body>
</html>